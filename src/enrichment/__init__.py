"""Waterfall enrichment package — Stage A (Places), B (Website), C (Socials).

Quality gates per stage (filters.py) ensure only eligible leads reach each
expensive API. Budget guards (budget.py) enforce monthly $ caps.

Run via run_enrichment.py orchestrator or each enrich_*.py individually.
"""
